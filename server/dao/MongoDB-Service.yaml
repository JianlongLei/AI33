#apiVersion: v1
#kind: Service
#metadata:
#  name: Title
#spec:
#  selector:
#    app: Title
#  ports:
#    - protocol: TCP
#      port: 80
#      targetPort: 8080
#  type: NodePort
# mongodb-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
spec:
  selector:
    app: mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP
